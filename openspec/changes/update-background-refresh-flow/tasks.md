## 1. 后台刷新状态
- [x] 1.1 在启动器中新增字段（如 `pending_background_config`、`pending_refresh_ts`）及辅助方法，用于在不触碰 UI 的情况下存储配置快照。
- [x] 1.2 扩展配置缓存工具，使后台线程可更新缓存与历史记录而无需触发 `update_ui_with_config`。

## 2. 调整后台流程
- [x] 2.1 修改 `auto_sync_completed`/`download_completed`（窗口隐藏路径），改为调用新的后台刷新方法并设置待刷新标记，而不是立即重建标签。
- [x] 2.2 排查其他隐藏窗口下的刷新入口（自动定时器、托盘同步等），统一改用该模式并记录延迟行为。

## 3. 恢复渲染
- [x] 3.1 在 `showEvent`/恢复入口检测 `pending_ui_refresh`，若存在则在聚焦搜索框前使用快照进行一次性重建。
- [x] 3.2 渲染成功后清理待刷新状态，并增加日志/指标以便观测。

## 4. 验证
- [x] 4.1 编写或扩展自动化覆盖（单测/集成）模拟隐藏窗口下的后台更新，确认 UI 会等待 `showEvent` 才刷新。
- [x] 4.2 手动验证或增加性能埋点，确保多次后台同步只触发一次重渲染，且窗口可见时依旧即时刷新。
